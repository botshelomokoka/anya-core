# Core dependencies
python-dotenv==1.0.0
typing==3.7.4.3

# Cryptography and security
cryptography==41.0.1
lnd-grpc==0.5.1

# Networking
twisted==22.10.0
kademlia==2.2.2

# Database (assuming SQLite is used, adjust if using a different database)
SQLAlchemy==2.0.15

# Testing
pytest==7.3.1

# JSON handling
simplejson==3.19.1

# Web5 (if it's actually used in the project)
# web5==0.1.0  # Uncomment if Web5 is actually used

# Additional utilities
random2==1.0.1

# Machine Learning (for ml_logic.py)
scikit-learn==1.2.2
numpy==1.24.3
pandas==2.0.2
tensorflow==2.12.0
nltk==3.8.1

# Network Discovery (for network_discovery.py)
scapy==2.5.0
netifaces==0.11.0

# Main System (for main_system.py)
aiohttp==3.8.4

# Additional common libraries that might be used across these modules
requests==2.31.0
beautifulsoup4==4.12.2
matplotlib==3.7.1

# User management (assuming it's a custom module)
# user_management  # This should be your local module, not a PyPI package

# Additional dependencies from setup_project.py
# subprocess32 is removed as it's for Python 2.7, and we're using Python 3

# Additional dependencies for user_management.py
# Add any specific dependencies required by user_management.py here
# For example:
# bcrypt==4.0.1  # If you're using bcrypt for password hashing
# pyjwt==2.6.0  # If you're using JWT for authentication
